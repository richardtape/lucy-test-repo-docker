# Database Configuration
DB_HOST=db-primary
DB_NAME=cms_db
DB_USER=your_user
DB_PASSWORD=your_password
REPL_USER=replica
REPL_PASSWORD=replica_pass
MARIADB_ROOT_PASSWORD=root_password

# NFS Configuration
# Fixed IP required for NFS-Ganesha mounting consistency
NFS_IP=172.28.0.10
NFS_SUBNET=172.28.0.0/16

# Docker Image Tag
# Use 'dev' for locally built images
IMAGE_TAG=dev
